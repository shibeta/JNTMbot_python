# 开启调试模式，日志输出将非常详细
debug: false
# Steam Bot后端的监听地址
steamBotHost: 127.0.0.1
# Steam Bot后端的监听端口
steamBotPort: 13091
# 访问Steam Bot后端的认证Token
steamBotToken: '0x4445414442454546'
# Steam Bot使用的代理，格式为"http://127.0.0.1:8080"或"socks5h://127.0.0.1:1080"。"system"表示使用系统代理，留空则不使用代理。建议使用HTTP代理
steamBotProxy: system
# 要发送消息的Steam群组ID，程序启动时可以读取到
steamGroupId: '37660928'
# 要发送消息的Steam群组频道ID，程序启动时可以读取到
steamChannelId: '163168791'
# 是否改用备用方法发送Steam群组消息，该方法通过与Steam客户端GUI交互以发送消息
useAlterMessagingMethod: false
# 用备用方法发送群组消息时，群聊窗口标题关键字，支持正则
AlterMessagingMethodWindowTitle: 蠢人帮
# 启用健康检查，每间隔一段时间检查Bot上次向Steam发送信息的时间
enableHealthCheck: true
# 健康检查的频率，即两次健康检查之间等待的时间 (分钟)
healthCheckInterval: 10
# 基于Steam消息的健康检查判断阈值，如果发现Bot一段时间内未向Steam发送过信息，则认为Bot不可用 (分钟)
healthCheckSteamChatTimeoutThreshold: 60
# 健康检查发现Bot不可用时，是否退出程序
enableExitOnUnhealthy: false
# 是否启用微信推送bot状态信息。启用后，当程序运行一段时间后发生报错退出，或健康状态发生变化时，会向微信推送错误信息
enableWechatPush: false
# pushplus的API token，用于微信通知
pushplusToken: ''
# 微信推送报错退出的启用延迟。为节省API用量，只有程序运行时长超过该时间后，报错退出时才会向微信推送 (分钟)
pushActivationDelay: 5
# 主循环连续报错的阈值，连续报错超过该次数将报错退出。设置为<=1则报错一次即退出
mainLoopConsecutiveErrorThreshold: 10
# 初始化GTA时重启GTA失败的阈值，连续重启失败超过该次数将抛出异常。设置为<=1则重启失败立即抛出异常
restartGTAConsecutiveFailThreshold: 5
# 卡单持续时间 (秒)
suspendGTATime: 13
# 面板消失后，卡单延迟时间 (秒)
delaySuspendTimePanelDisappear: 5
# 任务启动玩家落地后，卡单延迟时间 (秒)
delaySuspendTimeJobStart: 15
# 当bot变成问题玩家后是否自动挂机清除恶意值。设置为False后变成问题玩家会退出程序。
autoReduceBadSportOnDodgyPlayer: true
# 禁用在事务所内起床后自动移动到任务触发点，改为要求用户手动将角色移动到任务触发点
manualMoveToPoint: false
# 全部玩家已加入时立即开始差事而不等待 (绕过 "startMatchDelay" 时间)
startOnAllJoined: true
# 生活层进行"走到床头柱子前卡住"动作的持续时间 (毫秒)
walkToPillarTime: 1500
# 生活层进行"走到个人空间门口"动作的持续时间 (毫秒)
walkToBedroomEntranceTime: 5500
# 生活层进行"走出个人空间的门"动作时，向右后方移动的持续时间 (毫秒)
exitBedroomDoorBackTime: 1500
# 生活层进行"走出个人空间的门"动作时，向右前方移动的持续时间 (毫秒)
exitBedroomDoorForwardTime: 1000
# 生活层进行"走到楼梯门口"动作的持续时间 (毫秒)
walkToStairwellTime: 700
# 生活层进行"走进楼梯门"动作的持续时间 (毫秒)
enterStairwellTime: 2300
# 楼梯间进行"走前半截楼梯"动作的持续时间 (毫秒)
goDownFirstStairFlightTime: 4000
# 楼梯间进行"穿过楼梯中间的平台"动作的持续时间 (毫秒)
crossStairLandingTime: 1500
# 楼梯间进行"走后半截楼梯"动作的持续时间 (毫秒)
goDownSecondStairFlightTime: 4500
# 差事层进行"走出楼梯间"动作的持续时间 (毫秒)
exitStairwellTime: 1000
# 差事层进行"穿过走廊"动作的持续时间 (毫秒)
crossAisleTime: 4250
# 差事层进行"寻找差事黄圈"动作时 每次移动的持续时间 (毫秒)
moveTimeFindJob: 350
# 差事面板玩家加入状态检测间隔时间 (秒)
lobbyCheckLoopTime: 1
# 面板无人加入时重开时间 (秒)
matchPanelTimeout: 180
# 等待正在加入玩家超时重开时间 (秒)
playerJoiningTimeout: 120
# 开始差事等待延迟 (秒)
startMatchDelay: 15
# 等待差事启动落地超时时间 (秒)(防止卡在启动战局中)
exitMatchTimeout: 120
# 等待在事务所复活超时时间 (秒)(防止切换战局时卡云)
respawnInAgencyTimeout: 120
# 等待从线下进入线上和通过Steam加入战局落地超时时间 (秒)(防止在线模式加载时卡云)
onlineModeLoadingTimeout: 300
# RapidOCR的启动参数
ocrArgs: --models=".\models" --det=ch_PP-OCRv4_det_infer.onnx 
  --cls=ch_ppocr_mobile_v2.0_cls_infer.onnx --rec=rec_ch_PP-OCRv4_infer.onnx 
  --keys=dict_chinese.txt --padding=70 --maxSideLen=1024 --boxScoreThresh=0.5 
  --boxThresh=0.3 --unClipRatio=1.6 --doAngle=0 --mostAngle=0 --numThread=1
# 开好面板时发的消息 (设置为空字符串则不发这条消息)
msgOpenJobPanel: 德瑞差事已启动，请先看教程，学会卡CEO和卡单再进。如果无法连接请再试一次，bot没加速器网不好
# 没人加入超时重开时发的消息 (设置为空字符串则不发这条消息)
msgMatchPanelTimeout: 一直没有玩家加入，重新启动中
# 有人卡在正在加入超时重开时发的消息 (设置为空字符串则不发这条消息)
msgPlayerJoiningTimeout: 任务中含有卡B，重新启动中
# 满人时发的消息 (设置为空字符串则不发这条消息)
msgTeamFull: 满了，请等下一班车
# 差事启动时发的消息 (设置为空字符串则不发这条消息)
msgJobStarting: 即将发车，请在听到“咚”的一声后卡单
# 差事启动失败时发的消息 (设置为空字符串则不发这条消息)
msgJobStartFail: 启动差事失败，请等下一班车
# 发现有人没卡单时发的消息 (设置为空字符串则不发这条消息)
msgDetectedSB: 有人没有卡单，请先阅读教程，了解Bot的使用方法后再使用本bot
